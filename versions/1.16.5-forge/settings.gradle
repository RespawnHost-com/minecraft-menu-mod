pluginManagement {
    repositories {
        maven { url = 'https://maven.minecraftforge.net' }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'respawnhost-1.16.5-forge'

includeBuild('core-shim')
